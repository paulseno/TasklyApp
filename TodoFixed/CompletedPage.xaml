<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ToDo_App.CompletedPage"
             Title="Completed" BackgroundColor="#2b2539">
    <VerticalStackLayout Padding="16,10" Spacing="8">
        <Label Text="Completed Tasks" FontSize="22" FontAttributes="Bold"
               TextColor="White" HorizontalTextAlignment="Center" Margin="0,10,0,4"/>
        <Label x:Name="EmptyLabel" Text="No completed tasks yet."
               IsVisible="True" FontSize="15"
               HorizontalTextAlignment="Center" TextColor="#aaa"/>
        <ListView x:Name="completedLV" HasUnevenRows="True"
                  SeparatorVisibility="None" BackgroundColor="Transparent"
                  ItemTapped="completedLV_ItemTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Border Margin="0,4" Padding="14,10" BackgroundColor="#2a3a2a">
                            <Border.StrokeShape><RoundRectangle CornerRadius="12"/></Border.StrokeShape>
                            <Grid ColumnDefinitions="*,Auto">
                                <VerticalStackLayout>
                                    <Label Text="{Binding Title}" FontSize="16"
                                           FontAttributes="Bold" TextColor="#a5d6a7"/>
                                    <Label Text="{Binding Detail}" FontSize="13" TextColor="#bbb"/>
                                </VerticalStackLayout>
                                <Button Grid.Column="1" Text="🗑" FontSize="18"
                                        BackgroundColor="Transparent" TextColor="#f88"
                                        ClassId="{Binding Id}" Clicked="DeleteCompletedItem"/>
                            </Grid>
                        </Border>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </VerticalStackLayout>
</ContentPage>